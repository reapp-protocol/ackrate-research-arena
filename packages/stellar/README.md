# @ackrate/stellar

Typed Soroban bindings, network configuration, and signing helpers used by the
ackrate payment packages.

```bash
npm install @ackrate/stellar
```

This package preserves the deployed contract interface and network identifiers.
It does not rename or mutate any on-chain protocol values.

License: Apache-2.0.
